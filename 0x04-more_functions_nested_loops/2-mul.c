#include "holberton.h"
/**
 * mul(int a, int b) - main function
 *
 * Return: multiplication result
 *
 * @a: recives an integer
 * @b: recives an integer
 */
int mul(int a, int b)
{
	return (a * b);
}
